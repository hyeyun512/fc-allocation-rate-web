node_modules
.next
out
.env
.env.local
.vercel
*.log
*.tsbuildinfo

# 참고용으로 넣어둔 원본 엑셀 (민감 데이터, 저장소에 커밋하지 않음)
*.xlsx

# Outlook 메일 원본 (사내 문서, 저장소에 커밋하지 않음)
*.msg

# Supabase 테이블 백업 덤프 (배부율 실데이터, 저장소에 커밋하지 않음)
backup/
